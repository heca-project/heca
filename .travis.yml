language: rust
before_install:
  - sudo apt-get update
  - sudo apt-get install -y google-perftools  libgoogle-perftools-dev

rust:
  - nightly
  - stable

deploy:
  provider: script
  script: cargo test
  on:
    tags: true
